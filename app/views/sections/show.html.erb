<h1> Here are all the sections and their lessons: </h1>

<ul>
  <% sections.each do |section| %>
    <li><%= section.name %></li>
      <ul>
        <% section.lessons.each do |lesson| %>
          <li><%= lesson.name %></li>
        <% end %>
      </ul>
  <% end %>
</ul>
